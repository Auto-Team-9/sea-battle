## Дата: 2026-03-28

Pанималась вопросами drag drop. Поначалу думала взять какую-нибудь готовую библиотеку, но потом решила разобраться с нативным апи, чтобы не тащить лишнюю зависимость.

Для отслеживания перетаскивания два индекса: индекс, который тащим, и индекс, над которым сейчас находится. Смотрела какой-то очень короткий ролик на ютуб.
Из которого узнала, что нужен превент дефолт, иначе браузер не даст сделать дроп вообще (это меня задержало), долго не могла понять почему не происходит перетаскивание. Браузер считает элемент не подходящей целью для дропа и отменяет событие.

Вторая проблема была в том, что сначала сбрасывала индексы только в handleDrop, но если отпустить элемент мимо списка - onDrop не вызывается, а состояние зависает. Реешние - пришлось добавить сброс и в onDragEnd.

А ещё не подумала сразу о том, что для перемешивания вариантов ответа перед отображением массив ответов нужно копировать, а не мешать тот, который изначальный.

В апи добавила фильтрацию по типу вопроса, чтобы можно было загружать только order-вопросы для теста. Хотя логику для этого мы уже вряд ли успеем сделать.

Ещё написала скрипт  для загрузки вопросов в файрстор, чтобы он принимал любой файл с вопросами через аргумент командной строки.

Нашла документацию про виджет енжин,  можно будет добавить и пару других типов вопроса, а так же подправить архитерктуру, используя strategy. 
Но это чуть позже.

>За завтра нужно наклепать страницу кланов...Уж не знаю успею ли + Андрей не знаю будет ли добивать, нужно будет сделать механику выстрела, чтобы мои вопросы выводились...а то самой игры как таковой то и нет